{
  "name": "@chirag127/oriz-auth-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "oriz central auth — Clerk-powered SSO hub for *.oriz.in (account.oriz.in / auth.oriz.in).",
  "license": "MIT",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy:pages": "wrangler pages deploy dist --project-name=oriz-auth-app --branch=main --commit-dirty=true",
    "typecheck": "astro check"
  },
  "dependencies": {
    "@clerk/clerk-js": "^6.29.1",
    "astro": "^7.2.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2",
    "wrangler": "^4.123.0"
  }
}
